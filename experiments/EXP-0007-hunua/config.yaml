# EXP-0007: second scene from the brief -- Hunua clip5923 (439 frames; clip4982's frames were deleted from disk by Splats' clip driver after its training, so the scene moved to clip5923 (the only Hunua clip with frames + sparse + ply on 2026-09-06)).
# Same recipe as EXP-0003 full2 (trips mode, knn sizes, train_factor 1.0). No known shade
# frames here, so the report's dolly anchors on the first held-out image; treat the shade
# audit columns as not applicable for this scene.
scene_root: /Users/nzbirdranch/Splats/scenes/hunua/clips/clip5923_best
run_dir: output/runs/EXP-0007-hunua-clip5923/full-trips-2

width: 1008
crop: 384
mode: trips
layers: 5
epochs: 120
train_factor: 1.0

point_source:
  type: gaussian
  path: /Users/nzbirdranch/Splats/output/Training-Data/hunua/clip5923/clip5923_12000.ply
  min_opacity: 0.05
  size_mode: knn

device: mps
seed: 0
