# tools/

Helper scripts that support development but are not part of the installable
`trippy` Python package (that lives under `trippy/`). `fetch_upstream.sh`
(clones the upstream TRIPS repo and Zenodo checkpoints into `third_party/`,
which is gitignored) is created by another agent as part of the docs/
research track, not by this skeleton.
